"""Interaction feature helpers for main table."""

import pandas as pd


def add_interaction_features(df: pd.DataFrame) -> pd.DataFrame:
    """Phase 1 placeholder for lightweight interaction features."""
    featured = df.copy()
    # TODO: Add additional robust interactions in later phases.
    return featured
